const enum AppPage {
  home = '/home',
  team = '/team',
  authorization = '/authorization',
  book = '/book',
  audiocall = '/audiocall',
  sprint = '/sprint',
  statistics = '/statistics',
}

const enum Games {
  audio_call,
  sprint,
}

export {AppPage, Games};
